<!DOCTYPE html>
<html data-theme="winter" lang="pt-BR"> <!-- Tema alterado para winter -->
  <head>
    <title><%= content_for(:title) || "Minha Aplicação" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Fonte Inter importada do Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/daisyui@4.6.0/dist/full.min.css" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-base-200 flex flex-col min-h-screen box-border font-['Inter'] text-base-content"> <!-- Cor de texto padrão alterada para text-base-content -->
    <%# Notificação Flash com estilo Apple e cores ajustadas para o tema winter %>
    <% if flash[:notice] %>
      <div
        data-controller="notification"
        data-notification-delay-value="3500"
        class="toast toast-top toast-center transition transform duration-500 ease-out hidden z-50 p-4"
        data-transition-enter-from="opacity-0 -translate-y-full"
        data-transition-enter-to="opacity-100 translate-y-0"
        data-transition-leave-from="opacity-100 translate-y-0"
        data-transition-leave-to="opacity-0 -translate-y-full"
        role="alert"
      >
        <div class="toast toast-end">
          <div role="alert" class="alert alert-success rounded-xl shadow-lg bg-green-500 text-white"> <!-- Cores explícitas para notificação de sucesso -->
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 shrink-0 stroke-current" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
            <span class="font-medium"><%= flash[:notice][:message] || flash[:notice] %></span>
          </div>
        </div>
      </div>
    <% end %>

    <%= render "layouts/header" %>

    <main class="flex-grow">
      <%= yield %>
    </main>

    <%= render "layouts/footer" %>
  </body>
</html>